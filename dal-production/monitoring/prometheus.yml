global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'tranai-dal-prod'
    scrape_interval: 5s
    static_configs:
      - targets: ['host.docker.internal:3001'] # Scrape metrics from API server
